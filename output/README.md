# Output directory

Use this folder for generated artifacts (coverage, benchmarks, example outputs). Keep large or transient files here and out of version control; the root .gitignore excludes this path.

Canonical M1 result copies are available under `output/m1_*`.
